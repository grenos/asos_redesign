import React from 'react';
import { ScrollView } from 'react-native';

import ProductHero from './ProductHero';

export default () => {
  return (
    <ScrollView>
      <ProductHero />
    </ScrollView>
  );
};
